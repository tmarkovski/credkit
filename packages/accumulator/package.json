{
  "name": "@credkit/accumulator",
  "version": "0.1.0",
  "type": "module",
  "description": "VB positive accumulator over BLS12-381 for credential revocation: additions-static registry, deletion-only omega epoch updates, CDH weak-BB membership proofs for merged Fiat-Shamir challenges. Pure TypeScript, no WASM.",
  "license": "Unlicense",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tmarkovski/credkit.git",
    "directory": "packages/accumulator"
  },
  "main": "./src/index.ts",
  "files": [
    "src"
  ],
  "sideEffects": false,
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@credkit/bbs": "workspace:*",
    "@noble/curves": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
